# Recursive functions
# This will make an infinite loop so there should be a base condition to stop

def function():
  print("helow!!")
  function()      # the function call itself, which means it will keep calling itself indefinitely
function() 